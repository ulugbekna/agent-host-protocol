// <auto-generated>
// Generated from types/*.ts — do not edit.
//
// Regenerate with: npm run generate:dotnet
// </auto-generated>
#nullable enable

namespace Microsoft.AgentHostProtocol;

// ─── Enums ────────────────────────────────────────────────────────────

/// <summary>Discriminant for reconnect result types.</summary>
[JsonConverter(typeof(WireEnumConverter<ReconnectResultType>))]
public enum ReconnectResultType
{
    [WireValue("replay")]
    Replay,
    [WireValue("snapshot")]
    Snapshot,
}

/// <summary>How a new chat uses its source chat and turn.</summary>
[JsonConverter(typeof(WireEnumConverter<ChatSourceKind>))]
public enum ChatSourceKind
{
    /// <summary>Copy source history through the referenced turn into the new chat.</summary>
    [WireValue("fork")]
    Fork,
    /// <summary>Supply source context without copying it into the new chat's visible history.</summary>
    [WireValue("sideChat")]
    SideChat,
}

/// <summary>Encoding of fetched content data.</summary>
[JsonConverter(typeof(WireEnumConverter<ContentEncoding>))]
public enum ContentEncoding
{
    [WireValue("base64")]
    Base64,
    [WireValue("utf-8")]
    Utf8,
}

/// <summary>The kind of completion items being requested.</summary>
[JsonConverter(typeof(WireEnumConverter<CompletionItemKind>))]
public enum CompletionItemKind
{
    /// <summary>Completions for the text of a {@link Message} the user is composing.
    /// Each returned item carries an attachment that gets associated with the
    /// message when accepted.</summary>
    [WireValue("userMessage")]
    UserMessage,
}

/// <summary>Discriminant for {@link ResourceResolveResult.type}.</summary>
[JsonConverter(typeof(WireEnumConverter<ResourceType>))]
public enum ResourceType
{
    [WireValue("file")]
    File,
    [WireValue("directory")]
    Directory,
    [WireValue("symlink")]
    Symlink,
}

/// <summary>How {@link ResourceWriteParams.data} is placed within the target file.
///
/// Each mode interprets {@link ResourceWriteParams.position} differently:
///
/// - `truncate` (default): rooted at the **start** of the file. The file is
///   truncated at `position` (0 by default) and `data` is written from that
///   offset, so the resulting file is `existing[0..position] + data`. With
///   `position` omitted this is a full overwrite.
/// - `append`: rooted at the **end** of the file. `position` counts bytes
///   backwards from EOF, so `position: 0` (the default) writes at EOF —
///   POSIX append — and `position: 5` inserts `data` 5 bytes before the
///   current EOF, shifting those trailing 5 bytes after the inserted region.
///   The server MUST evaluate the effective EOF and write atomically with
///   respect to other appenders so concurrent `append` writes do not
///   clobber each other.
/// - `insert`: rooted at the **start** of the file. `position` (0 by default)
///   is the byte offset at which `data` is spliced in; bytes at or after
///   `position` are shifted right by `data.length`. `insert` always grows
///   the file — use `truncate` to overwrite bytes in place.</summary>
[JsonConverter(typeof(WireEnumConverter<ResourceWriteMode>))]
public enum ResourceWriteMode
{
    [WireValue("truncate")]
    Truncate,
    [WireValue("append")]
    Append,
    [WireValue("insert")]
    Insert,
}

// ─── Command Payloads ─────────────────────────────────────────────────

/// <summary>Establishes a new connection and negotiates the protocol version.
/// This MUST be the first message sent by the client.</summary>
public sealed record InitializeParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Protocol versions the client is willing to speak, ordered from most
    /// preferred to least preferred. Each entry is a [SemVer](https://semver.org)
    /// `MAJOR.MINOR.PATCH` string (e.g. `"0.1.0"`).
    ///
    /// The server selects one entry and returns it as `InitializeResult.protocolVersion`.
    /// If the server cannot speak any of the offered versions, it MUST return
    /// error code `-32005` (`UnsupportedProtocolVersion`) with required
    /// `UnsupportedProtocolVersionErrorData` containing `supportedVersions`.</summary>
    public required List<string> ProtocolVersions { get; init; }

    /// <summary>Unique client identifier</summary>
    public required string ClientId { get; init; }

    /// <summary>Optional identity of the client implementation (name and version).
    /// Informational only — see {@link Implementation} for how it may and may not
    /// be used. Distinct from {@link InitializeParams.clientId | `clientId`},
    /// which is an opaque per-connection identifier used for reconnection, not a
    /// human-readable implementation name.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Implementation? ClientInfo { get; init; }

    /// <summary>URIs to subscribe to during handshake</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public List<string>? InitialSubscriptions { get; init; }

    /// <summary>IETF BCP 47 language tag indicating the client's preferred locale
    /// (e.g. `"en-US"`, `"ja"`). The server SHOULD use this to localise
    /// user-facing strings such as confirmation option labels.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Locale { get; init; }

    /// <summary>Optional client capability declarations.
    ///
    /// Servers SHOULD only advertise features whose corresponding client
    /// capability is set here. Absent means "not declared" — the server
    /// MUST assume the client does not support the feature.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public ClientCapabilities? Capabilities { get; init; }
}

/// <summary>Result of the `initialize` command.
///
/// `protocolVersion` is the version the server has selected from the client's
/// `protocolVersions` list. The client and server MUST use this version for
/// the rest of the connection. If the server cannot speak any of the offered
/// versions it MUST return error code `-32005` (`UnsupportedProtocolVersion`)
/// with required `UnsupportedProtocolVersionErrorData` containing
/// `supportedVersions`, instead of a result.</summary>
public sealed record InitializeResult
{
    /// <summary>Protocol version selected by the server. MUST be one of the entries in
    /// `InitializeParams.protocolVersions`. Formatted as a [SemVer](https://semver.org)
    /// `MAJOR.MINOR.PATCH` string (e.g. `"0.1.0"`).</summary>
    public required string ProtocolVersion { get; init; }

    /// <summary>Current server sequence number</summary>
    public long ServerSeq { get; init; }

    /// <summary>Optional identity of the server implementation (name and version).
    /// Informational only — see {@link Implementation} for how it may and may not
    /// be used. Whereas {@link InitializeResult.protocolVersion | `protocolVersion`}
    /// identifies the negotiated protocol, `serverInfo` identifies the host
    /// software behind it.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Implementation? ServerInfo { get; init; }

    /// <summary>Optional implementation-specific extension metadata advertised by the host.
    ///
    /// Hosts and clients MAY agree on namespaced keys for capabilities that are not
    /// part of the standardized protocol. Clients MUST ignore keys they do not
    /// understand. Capabilities needed for interoperable behavior SHOULD use typed
    /// fields on {@link InitializeResult} instead.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Snapshots for each `initialSubscriptions` URI</summary>
    public required List<Snapshot> Snapshots { get; init; }

    /// <summary>Suggested default directory for remote filesystem browsing</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? DefaultDirectory { get; init; }

    /// <summary>Characters that, when typed in a {@link Message} input, SHOULD cause
    /// the client to issue a `completions` request with
    /// {@link CompletionItemKind.UserMessage}. Typically includes characters like
    /// `'@'` or `'/'`.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public List<string>? CompletionTriggerCharacters { get; init; }

    /// <summary>Prefix that the host recognizes at the start of a user {@link Message.text}
    /// as a shorthand for executing the remainder as a terminal command. Currently
    /// the standardized convention is `"!"`; absence means the host does not
    /// support command prefixes.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? TerminalCommandPrefix { get; init; }

    /// <summary>OTLP telemetry channels the host emits, if any. Each populated field is
    /// either a literal `ahp-otlp:` channel URI or an RFC 6570 URI template a
    /// client expands before subscribing (currently only the `logs` channel
    /// defines a template variable, `{level}`, for subscriber-side severity
    /// filtering). Clients MAY ignore signals they cannot process.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public TelemetryCapabilities? Telemetry { get; init; }

    /// <summary>Host-owned automation support. Presence means clients may subscribe to
    /// `ahp-automations://` for {@link AutomationState}; absence means the
    /// host does not expose an automation catalogue or automation commands.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public AutomationCapabilities? Automations { get; init; }

    /// <summary>Host/runtime-owned local-canvas support. Presence means the SERVER
    /// currently has a working runtime able to serve `openCanvas` /
    /// `invokeCanvasAction` for at least one qualifying (explicitly installed
    /// and trust-eligible) extension/package source; absence means the host
    /// has no available canvas runtime, and clients MUST treat every canvas as
    /// {@link CanvasAvailabilityStatus.Unsupported} regardless of what
    /// {@link ClientCapabilities.canvases} declared.
    ///
    /// **Protocol version support alone is not a runtime capability**: a host
    /// speaking protocol `&gt;= 0.10.0` without this field present MUST NOT be
    /// assumed to have a usable canvas runtime. This field — not the
    /// negotiated `protocolVersion` — is the authoritative signal, and is
    /// independent of any individual canvas's live availability
    /// ({@link CanvasAvailabilityState}) or trust decision
    /// ({@link CanvasTrustState}).</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public CanvasCapabilities? Canvases { get; init; }
}

/// <summary>Identifies a protocol implementation — the software (and build) on one end
/// of the connection, as distinct from the {@link AgentInfo | agent persona} it
/// hosts. Carried as {@link InitializeParams.clientInfo | `clientInfo`} on the
/// client side and {@link InitializeResult.serverInfo | `serverInfo`} on the
/// server side, mirroring LSP's `clientInfo`/`serverInfo` and MCP's
/// `Implementation`.
///
/// This is **informational only**: it exists for logging, telemetry, an
/// about/status affordance, and — as a last resort — a known-issue workaround
/// for a specific buggy build. It is **not** a feature-detection mechanism.
/// Feature availability stays with the capability model
/// ({@link ClientCapabilities} and the various `*.capabilities` declarations);
/// implementations SHOULD NOT gate protocol behaviour on parsing
/// {@link Implementation.version | `version`}.</summary>
public sealed record Implementation
{
    /// <summary>Implementation name, e.g. a product or package identifier.</summary>
    public required string Name { get; init; }

    /// <summary>Implementation version. A [SemVer](https://semver.org) string is
    /// recommended but not required.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Version { get; init; }

    /// <summary>Optional human-readable display name.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Title { get; init; }
}

/// <summary>Optional capabilities a client declares during `initialize`.
///
/// Each field is a presence flag: an empty object `{}` means "supported",
/// absence means "not supported". Sub-fields on individual capabilities
/// are reserved for future per-capability options.</summary>
public sealed record ClientCapabilities
{
    /// <summary>Client can render
    /// [MCP Apps](https://github.com/modelcontextprotocol/ext-apps) — i.e.
    /// it can host the View sandbox, run the `ui/*` protocol against it,
    /// and forward `mcp://`-channel traffic on the App's behalf.
    ///
    /// Hosts SHOULD only populate
    /// {@link McpServerCustomization.mcpApp | `McpServerCustomization.mcpApp`}
    /// (and expose the corresponding
    /// {@link McpServerCustomization.channel | `mcp://` channel}) when this
    /// capability is declared. Clients that omit it MUST treat
    /// App-bearing tool calls as ordinary MCP tool calls.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? McpApps { get; init; }

    /// <summary>Client can render local canvases: `listCanvasTypes`, `openCanvas`,
    /// subscribe to the resulting `ahp-canvas:` channel, and drive
    /// `resolveCanvasSource` / `invokeCanvasAction` / `restartCanvasProvider` /
    /// `closeCanvas`.
    ///
    /// Hosts SHOULD NOT offer canvas admission to a client that omits this
    /// capability; such a client MUST be treated as if every canvas were
    /// {@link CanvasAvailabilityStatus.Unsupported}. Omission does not imply
    /// anything about server/runtime execution trust — see
    /// {@link CanvasTrustStatus}, which is a separate, host-owned decision.
    ///
    /// This declares only the CLIENT's rendering capability. Protocol version
    /// support alone (i.e. speaking &gt;= 0.10.0) is not evidence that the SERVER
    /// actually has a working canvas runtime — see
    /// {@link InitializeResult.canvases}, the server-side counterpart, which a
    /// client MUST also check before treating canvases as usable.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Canvases { get; init; }
}

/// <summary>Automation features supported by this host authority.
///
/// The presence of this object advertises the baseline `ahp-automations://`
/// catalogue. Optional fields describe additional host features and
/// restrictions.
///
/// Capabilities describe implementation support.
/// {@link AutomationEntry.operations} remains authoritative for which
/// definition mutations are currently allowed on a particular automation.</summary>
public sealed record AutomationCapabilities
{
    /// <summary>Present when clients may dispatch {@link AutomationCreateRequestedAction}.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public AutomationCreateCapability? Create { get; init; }

    /// <summary>Present when definitions may contain {@link AutomationScheduleTrigger | schedule triggers}.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public AutomationScheduleCapabilities? Schedules { get; init; }

    /// <summary>Present when clients may request cancellation of `pending` or `running`
    /// automation runs.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public AutomationRunCancellationCapability? RunCancellation { get; init; }

    /// <summary>Maximum terminal entries retained in {@link AutomationEntry.runs}. Active
    /// runs are not counted toward the limit. Absence means the retention limit is
    /// implementation-defined.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? RunHistoryLimit { get; init; }
}

/// <summary>Local-canvas runtime features supported by this host authority. The empty
/// object means "supported" — see {@link InitializeResult.canvases} for what
/// presence/absence of this field itself means.</summary>
public sealed record CanvasCapabilities
{
}

/// <summary>Presence capability for {@link AutomationCreateRequestedAction |
/// `automation/createRequested`}.
///
/// The empty object means "supported"; fields are reserved for future
/// create-specific options.</summary>
public sealed record AutomationCreateCapability
{
}

/// <summary>Host restrictions on portable {@link AutomationSchedule} triggers.
///
/// The cron grammar itself is fixed by AHP. Hosts MUST accept every expression
/// in that grammar unless it violates an advertised interval restriction.</summary>
public sealed record AutomationScheduleCapabilities
{
    /// <summary>Smallest permitted interval between consecutive occurrences produced by
    /// {@link AutomationSchedule.expression}. Omission means no restriction beyond
    /// the cron format's one-minute resolution.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? MinIntervalMinutes { get; init; }
}

/// <summary>Presence capability for {@link AutomationRunCancelRequestedAction |
/// `automationRun/cancelRequested`}.
///
/// The empty object means "supported." Clients may dispatch the action for
/// `pending` or `running` runs; terminal runs cannot be cancelled.</summary>
public sealed record AutomationRunCancellationCapability
{
}

/// <summary>Re-establishes a dropped connection. The server replays missed actions or
/// provides fresh snapshots.</summary>
public sealed record ReconnectParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Client identifier from the original connection</summary>
    public required string ClientId { get; init; }

    /// <summary>Last `serverSeq` the client received</summary>
    public long LastSeenServerSeq { get; init; }

    /// <summary>URIs the client was subscribed to</summary>
    public required List<string> Subscriptions { get; init; }
}

/// <summary>Reconnect result when the server can replay from the requested sequence.
///
/// The server MUST include all replayed data in the response.</summary>
public sealed record ReconnectReplayResult
{
    /// <summary>Discriminant</summary>
    public ReconnectResultType Type { get; init; }

    /// <summary>Missed action envelopes since `lastSeenServerSeq`</summary>
    public required List<ActionEnvelope> Actions { get; init; }

    /// <summary>URIs from `ReconnectParams.subscriptions` that the server cannot resume.
    /// This includes resources that no longer exist (e.g. disposed sessions or
    /// terminals) as well as resources the client is no longer permitted to
    /// observe. Clients SHOULD drop these from their local subscription set.</summary>
    public required List<string> Missing { get; init; }
}

/// <summary>Reconnect result when the gap exceeds the replay buffer.</summary>
public sealed record ReconnectSnapshotResult
{
    /// <summary>Discriminant</summary>
    public ReconnectResultType Type { get; init; }

    /// <summary>Fresh snapshots for each subscription</summary>
    public required List<Snapshot> Snapshots { get; init; }
}

/// <summary>Subscribe to a URI-identified channel.
///
/// A channel MAY have state associated with it (e.g. root, sessions,
/// terminals) or be stateless (pure pub/sub for streaming data). For
/// state-bearing channels the result includes a snapshot; for stateless
/// channels `snapshot` is omitted.</summary>
public sealed record SubscribeParams
{
    /// <summary>Channel URI this command targets.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Optional delivery preferences for this subscription.
    ///
    /// Servers MAY use these preferences to buffer and coalesce high-frequency
    /// updates while preserving the same reduced state. Omit this field for the
    /// server's default delivery behavior.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public SubscriptionDeliveryOptions? Delivery { get; init; }

    /// <summary>Optional client-requested shape for the returned snapshot.
    ///
    /// Servers that do not understand a requested view ignore it and return their
    /// default snapshot. Clients MUST tolerate receiving more state than requested.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public SubscribeView? View { get; init; }
}

/// <summary>Optional client-requested shape for a subscription snapshot.</summary>
public sealed record SubscribeView
{
    /// <summary>Advisory number of most-recent completed turns to expose in a chat
    /// snapshot.
    ///
    /// Servers MAY return more or fewer turns than requested. When omitted, the
    /// host MUST return all retained turns. When older turns remain available, the
    /// returned {@link ChatState} carries `turnsNextCursor`; clients pass that
    /// cursor to `fetchTurns` to ask the host to page more turns into the chat
    /// state.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? Turns { get; init; }
}

/// <summary>Advisory delivery preferences for a single subscription.</summary>
public sealed record SubscriptionDeliveryOptions
{
    /// <summary>Maximum time, in milliseconds, that the server may intentionally delay
    /// delivery while buffering/coalescing updates for this subscription.
    ///
    /// A value of `0` requests immediate delivery with no intentional coalescing.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? MaxLatencyMs { get; init; }
}

/// <summary>Result of the `subscribe` command.
///
/// `snapshot` is present when the subscribed channel has associated state, and
/// absent for stateless channels.</summary>
public sealed record SubscribeResult
{
    /// <summary>Snapshot of the subscribed channel's state (omitted for stateless channels)</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Snapshot? Snapshot { get; init; }
}

// TODO: could not generate SessionForkSource: Error: Interface SessionForkSource not found

/// <summary>Creates a new session with the specified agent provider.
///
/// If the session URI already exists, the server MUST return an error with code
/// `-32003` (`SessionAlreadyExists`).
///
/// After creation, the client should subscribe to the session URI to receive state
/// updates. The server also broadcasts a `root/sessionAdded` notification to all
/// clients.</summary>
public sealed record CreateSessionParams
{
    /// <summary>Session URI (client-chosen, e.g. `ahp-session:/&lt;uuid&gt;`)</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Agent provider ID</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Provider { get; init; }

    /// <summary>The working directories the session's agent is granted tool access to.
    /// A session may span multiple directories; they are equal peers except when
    /// the agent advertises a protected-primary capability. An
    /// {@link MultipleWorkingDirectoriesCapability.immutablePrimary | immutable
    /// primary} is fixed, while a
    /// {@link MultipleWorkingDirectoriesCapability.primaryReplacement | replaceable
    /// primary} is changed only with `session/workingDirectoryReplaced`.
    ///
    /// A client MUST NOT supply more than one entry unless the agent advertises
    /// {@link AgentCapabilities.multipleWorkingDirectories}; a server without that
    /// capability treats only the first entry as the session's working directory
    /// and ignores the rest. Dispatch working-directory actions to change the set
    /// after the session has started.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public List<string>? WorkingDirectories { get; init; }

    /// <summary>Agent-specific configuration values collected via `resolveSessionConfig`.
    /// Keys and values correspond to the schema returned by the server.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Config { get; init; }

    /// <summary>Eagerly claim an active client role for the new session.
    ///
    /// When provided, the server initializes the session with this client as an
    /// active client, equivalent to dispatching a `session/activeClientSet`
    /// action immediately after creation. The `clientId` MUST match the
    /// `clientId` the creating client supplied in `initialize`.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public SessionActiveClient? ActiveClient { get; init; }

    /// <summary>Opt-in progress token. When set, the client is offering to receive
    /// `progress` notifications (see `ProgressParams`) for any long-running work
    /// the server does to bring this session up — most notably the lazy,
    /// first-use download of the provider's native SDK. The server echoes this
    /// exact token on every `progress` frame so the client can correlate it to
    /// this `createSession` call (and the UI awaiting it).
    ///
    /// The token MUST be unique across the client's active requests. The server
    /// MAY ignore it (e.g. when nothing long-running is needed), in which case no
    /// `progress` notifications are emitted.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? ProgressToken { get; init; }
}

/// <summary>Disposes a session and cleans up server-side resources.
///
/// The server broadcasts a `root/sessionRemoved` notification to all clients.</summary>
public sealed record DisposeSessionParams
{
    /// <summary>Channel URI this command targets.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }
}

/// <summary>Copies source history through a completed turn into the new chat.</summary>
public sealed record ForkChatSource
{
    /// <summary>Discriminant</summary>
    public ChatSourceKind Kind { get; init; }

    /// <summary>URI of the existing source chat.</summary>
    public required string Chat { get; init; }

    /// <summary>Completed turn identifier in the source chat.
    ///
    /// Content through this turn is copied into the new chat's visible `turns`.</summary>
    public required string TurnId { get; init; }
}

/// <summary>Supplies source context to a new side chat without copying it into the side
/// chat's visible history.</summary>
public sealed record SideChatSource
{
    /// <summary>Discriminant</summary>
    public ChatSourceKind Kind { get; init; }

    /// <summary>URI of the existing source chat.</summary>
    public required string Chat { get; init; }

    /// <summary>Stable source-turn identifier in the source chat.
    ///
    /// Hosts resolve this id against the source chat's current `activeTurn` or its
    /// retained `turns` when accepting `createChat`. If it names the current
    /// active turn, the host snapshots the source chat's retained history plus
    /// that turn's current user message and any partial assistant response already
    /// available. Once that turn later becomes historical, it is still referenced
    /// by this same identifier.</summary>
    public required string TurnId { get; init; }

    /// <summary>Optional immutable selected-text snapshot to carry into the created side
    /// chat's origin.
    ///
    /// When present, the host MUST snapshot and preserve this exact selection when
    /// it accepts `createChat`; later source-turn deltas do not alter it.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public SideChatSelection? Selection { get; init; }
}

/// <summary>Creates a new chat within a session.</summary>
public sealed record CreateChatParams
{
    /// <summary>Session URI containing the new chat.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Chat URI (client-chosen, e.g. `ahp-chat:/&lt;uuid&gt;`).</summary>
    public required string Chat { get; init; }

    /// <summary>Optional initial message for the new chat.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Message? InitialMessage { get; init; }

    /// <summary>Optional source chat and source turn.
    ///
    /// The source chat MUST belong to this session. Clients MUST only request
    /// `kind: "fork"` when the selected agent advertises
    /// `capabilities.multipleChats.fork`, and `kind: "sideChat"` when the
    /// selected agent advertises `capabilities.multipleChats.sideChat`. Both
    /// source forms carry a stable top-level `turnId`. Forks target completed
    /// turns. Side chats also carry a stable `turnId`, which the host resolves
    /// against the source chat's current active turn or retained history. If it
    /// resolves to the active turn, the host snapshots the currently available
    /// partial response when accepting `createChat`. When
    /// `source.kind === "sideChat"` and `source.selection` is present, the host
    /// also snapshots and preserves that exact selected text in the created chat's
    /// origin; any `responsePartId` there is provenance only, not a live range.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public ChatSource? Source { get; init; }

    /// <summary>Initial working-directory subset for this chat. Every entry MUST be
    /// present in the owning session's `workingDirectories`; the server MUST
    /// reject any entry that is not. When absent, the chat inherits the full
    /// session set. Forked chats (those whose `source.kind` is `"fork"`) inherit
    /// the source chat's `workingDirectories`; this field is ignored for forks.
    ///
    /// A client MUST NOT supply this field unless the agent advertises
    /// {@link AgentCapabilities.multipleWorkingDirectories}.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public List<string>? WorkingDirectories { get; init; }
}

/// <summary>Disposes a chat and cleans up server-side resources.</summary>
public sealed record DisposeChatParams
{
    /// <summary>Channel URI this command targets.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }
}

/// <summary>Returns a list of session summaries. Used to populate session lists and sidebars.
///
/// The session list is **not** part of the state tree because it can be arbitrarily
/// large. Clients fetch it imperatively and maintain a local cache updated by
/// `root/sessionAdded` and `root/sessionRemoved` notifications.
///
/// A large catalogue can be fetched incrementally via the {@link PaginatedParams}
/// `limit`/`cursor` inputs (see that type for the full pagination contract). The
/// server SHOULD return most-recently-modified entries first, so the first page
/// is the immediately useful one. The `root/session*` notifications keep an
/// already-fetched page live; pagination governs only the initial and backfill
/// fetches.</summary>
public sealed record ListSessionsParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Maximum number of entries to return in this page. The server SHOULD respect
    /// this bound but MAY return fewer entries and MAY impose its own upper cap.
    /// Omit to let the server choose the page size.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? Limit { get; init; }

    /// <summary>Opaque pagination cursor from a previous {@link PaginatedResult.nextCursor}.
    /// Omit to fetch the first page. Cursors are server-defined and MUST be treated
    /// as opaque — do not parse, modify, or persist them across connections. An
    /// unrecognised cursor SHOULD be rejected with an `InvalidParams` error.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Cursor { get; init; }
}

/// <summary>Result of the `listSessions` command.</summary>
public sealed record ListSessionsResult
{
    /// <summary>Opaque cursor for the next page. Present when more entries exist beyond the
    /// returned page; absent signals the end of the collection. Pass it back as
    /// {@link PaginatedParams.cursor} to fetch the following page.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? NextCursor { get; init; }

    /// <summary>The list of session summaries. The server SHOULD order them
    /// most-recently-modified first.</summary>
    public required List<SessionSummary> Items { get; init; }
}

/// <summary>Reads the content of a resource by URI.
///
/// Content references keep the state tree small by storing large data (images,
/// long tool outputs) by reference rather than inline.
///
/// Binary content (images, etc.) MUST use `base64` encoding. Text content MAY
/// use `utf-8` encoding.
///
/// Like all `resource*` methods, `resourceRead` is symmetrical and MAY be
/// sent in either direction. Hosts use it to fetch content from a
/// client-published URI (e.g. `virtual://my-client/...` plugins); clients
/// use it to read host-side files. The receiver enforces access via the
/// same permission/`resourceRequest` flow regardless of which peer initiated.</summary>
public sealed record ResourceReadParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Content URI from a `ContentRef`</summary>
    public required string Uri { get; init; }

    /// <summary>Preferred encoding for the returned data (default: server-chosen)</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public ContentEncoding? Encoding { get; init; }
}

/// <summary>Result of the `resourceRead` command.
///
/// The server SHOULD honor the `encoding` requested in the params. If the
/// server cannot provide the requested encoding, it MUST fall back to either
/// `base64` or `utf-8`.</summary>
public sealed record ResourceReadResult
{
    /// <summary>Content encoded as a string</summary>
    public required string Data { get; init; }

    /// <summary>How `data` is encoded</summary>
    public ContentEncoding Encoding { get; init; }

    /// <summary>Content type (e.g. `"image/png"`, `"text/plain"`)</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? ContentType { get; init; }
}

/// <summary>Writes content to a file on the server's filesystem.
///
/// Binary content (images, etc.) MUST use `base64` encoding. Text content MAY
/// use `utf-8` encoding.
///
/// If the file does not exist, it is created. If the file already exists, the
/// effect on existing bytes depends on {@link ResourceWriteParams.mode}:
/// `truncate` (default) overwrites from the chosen offset onward, `append`
/// preserves all existing bytes and adds `data` at a position rooted at EOF,
/// and `insert` preserves all existing bytes and splices `data` in at an
/// offset rooted at the start of the file.
///
/// Like all `resource*` methods, `resourceWrite` is symmetrical and MAY be
/// sent in either direction.</summary>
public sealed record ResourceWriteParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Target file URI on the server filesystem</summary>
    public required string Uri { get; init; }

    /// <summary>Content encoded as a string</summary>
    public required string Data { get; init; }

    /// <summary>How `data` is encoded</summary>
    public ContentEncoding Encoding { get; init; }

    /// <summary>Content type (e.g. `"text/plain"`, `"image/png"`)</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? ContentType { get; init; }

    /// <summary>If `true`, the server MUST fail if the file already exists instead of
    /// overwriting it. Useful for safe creation of new files.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public bool? CreateOnly { get; init; }

    /// <summary>How `data` is placed within the target file. Defaults to `'truncate'`
    /// (full overwrite) when omitted. See {@link ResourceWriteMode} for the
    /// meaning of each mode and how it interprets {@link position}.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public ResourceWriteMode? Mode { get; init; }

    /// <summary>Byte offset interpreted according to {@link mode}. Defaults to `0`.
    /// - `truncate`: offset from the start of the file at which to truncate
    ///   before writing.
    /// - `append`: bytes back from EOF at which to insert `data`.
    /// - `insert`: offset from the start of the file at which to splice in
    ///   `data`.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? Position { get; init; }

    /// <summary>Optimistic-concurrency token previously returned by
    /// {@link ResourceResolveResult.etag}. When set, the server MUST fail with
    /// `Conflict` if the current `etag` does not match — preventing lost
    /// updates between a `resourceResolve` and a subsequent `resourceWrite`.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? IfMatch { get; init; }
}

/// <summary>Result of the `resourceWrite` command.
///
/// An empty object on success.</summary>
public sealed record ResourceWriteResult
{
}

/// <summary>Lists directory entries at a file URI on the server's filesystem.
///
/// This is intended for remote folder pickers and similar UI that needs to let
/// users navigate the server's local filesystem.
///
/// The server MUST return success only if the target exists and is a directory.
/// If the target does not exist, is not a directory, or cannot be accessed, the
/// server MUST return a JSON-RPC error.
///
/// Like all `resource*` methods, `resourceList` is symmetrical and MAY be
/// sent in either direction.</summary>
public sealed record ResourceListParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Directory URI on the server filesystem</summary>
    public required string Uri { get; init; }
}

/// <summary>Result of the `resourceList` command.</summary>
public sealed record ResourceListResult
{
    /// <summary>Entries directly contained in the requested directory</summary>
    public required List<DirectoryEntry> Entries { get; init; }
}

/// <summary>Directory entry returned by `resourceList`.</summary>
public sealed record DirectoryEntry
{
    /// <summary>Base name of the entry</summary>
    public required string Name { get; init; }

    /// <summary>Whether the entry is a file or directory</summary>
    public required string Type { get; init; }
}

/// <summary>Copies a resource from one URI to another on the server's filesystem.
///
/// If the destination already exists, it is overwritten unless `failIfExists`
/// is set.
///
/// Like all `resource*` methods, `resourceCopy` is symmetrical and MAY be
/// sent in either direction.</summary>
public sealed record ResourceCopyParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Source URI to copy from</summary>
    public required string Source { get; init; }

    /// <summary>Destination URI to copy to</summary>
    public required string Destination { get; init; }

    /// <summary>If `true`, the server MUST fail if the destination already exists instead
    /// of overwriting it.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public bool? FailIfExists { get; init; }
}

/// <summary>Result of the `resourceCopy` command.
///
/// An empty object on success.</summary>
public sealed record ResourceCopyResult
{
}

/// <summary>Deletes a resource at a URI on the server's filesystem.
///
/// Like all `resource*` methods, `resourceDelete` is symmetrical and MAY be
/// sent in either direction.</summary>
public sealed record ResourceDeleteParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>URI of the resource to delete</summary>
    public required string Uri { get; init; }

    /// <summary>If `true` and the target is a directory, delete it and all its contents
    /// recursively. If `false` (default), deleting a non-empty directory MUST fail.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public bool? Recursive { get; init; }
}

/// <summary>Result of the `resourceDelete` command.
///
/// An empty object on success.</summary>
public sealed record ResourceDeleteResult
{
}

/// <summary>Moves (renames) a resource from one URI to another on the server's filesystem.
///
/// If the destination already exists, it is overwritten unless `failIfExists`
/// is set.
///
/// Like all `resource*` methods, `resourceMove` is symmetrical and MAY be
/// sent in either direction.</summary>
public sealed record ResourceMoveParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Source URI to move from</summary>
    public required string Source { get; init; }

    /// <summary>Destination URI to move to</summary>
    public required string Destination { get; init; }

    /// <summary>If `true`, the server MUST fail if the destination already exists instead
    /// of overwriting it.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public bool? FailIfExists { get; init; }
}

/// <summary>Result of the `resourceMove` command.
///
/// An empty object on success.</summary>
public sealed record ResourceMoveResult
{
}

/// <summary>Resolves a resource — the combination of POSIX `stat` and `realpath`.
///
/// `resourceResolve` returns metadata about the resource together with its
/// canonical URI after symlink resolution. Use this in place of any
/// `resourceExists` shim: a missing resource MUST surface as a `NotFound`
/// JSON-RPC error rather than a success with a sentinel value. Callers that
/// truly need a boolean check should attempt `resourceResolve` and treat
/// `NotFound` as "does not exist".
///
/// Like all `resource*` methods, `resourceResolve` is symmetrical and MAY be
/// sent in either direction.</summary>
public sealed record ResourceResolveParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>URI to resolve</summary>
    public required string Uri { get; init; }

    /// <summary>When `true` (default), follow symlinks and report the metadata of the
    /// link target — and set `uri` in the result to the canonical (realpath)
    /// URI. When `false`, stat the link itself (lstat semantics) and report
    /// `type: 'symlink'`.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public bool? FollowSymlinks { get; init; }
}

/// <summary>Result of the `resourceResolve` command.</summary>
public sealed record ResourceResolveResult
{
    /// <summary>Canonical URI after symlink resolution. Equal to the requested URI when
    /// `followSymlinks` is `false` or the URI does not traverse a symlink.</summary>
    public required string Uri { get; init; }

    /// <summary>Resource kind.</summary>
    public ResourceType Type { get; init; }

    /// <summary>Size in bytes. Omitted for directories when the provider cannot
    /// cheaply compute it.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? Size { get; init; }

    /// <summary>Last-modified time in ISO 8601 format, when known.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Mtime { get; init; }

    /// <summary>Creation time in ISO 8601 format, when known.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Ctime { get; init; }

    /// <summary>Sniffed MIME type, when known (e.g. `"text/plain"`, `"image/png"`).</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? ContentType { get; init; }

    /// <summary>Opaque per-provider version token. When present, pass it as
    /// {@link ResourceWriteParams.ifMatch} on a subsequent `resourceWrite` to
    /// detect concurrent modifications.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Etag { get; init; }
}

/// <summary>Creates a directory on the server's filesystem with `mkdir -p` semantics.
///
/// The server MUST create any missing parent directories. Creating a
/// directory that already exists is a no-op success. If `uri` already
/// exists but is **not** a directory, the server MUST fail with
/// `AlreadyExists`.
///
/// Like all `resource*` methods, `resourceMkdir` is symmetrical and MAY be
/// sent in either direction.</summary>
public sealed record ResourceMkdirParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Directory URI to create (parents created as needed).</summary>
    public required string Uri { get; init; }
}

/// <summary>Result of the `resourceMkdir` command.
///
/// An empty object on success.</summary>
public sealed record ResourceMkdirResult
{
}

/// <summary>Requests permission to access a resource on the receiver's filesystem.
///
/// `resourceRequest` is symmetrical and MAY be sent in either direction: a
/// client asks the server to grant access to a server-side resource, or a
/// server asks the client to grant access to a client-side resource. The
/// receiver decides whether to allow, deny, or prompt the user for the
/// requested access.
///
/// If the receiver denies access, it MUST respond with `PermissionDenied`
/// (-32009). The error data MAY include a `ResourceRequestParams` value
/// describing the access the caller would need to be granted for the
/// operation to succeed; see `PermissionDeniedErrorData` in
/// `types/errors.ts`.
///
/// After a successful `resourceRequest`, the caller MAY use the corresponding
/// `resource*` commands (e.g. `resourceRead`, `resourceWrite`) to perform the
/// operation. Receivers MAY rescind access at any time by returning
/// `PermissionDenied` on subsequent operations.
///
/// Either `read`, `write`, or both SHOULD be set to `true`. A request with
/// neither flag set is treated as `read: true` by receivers.</summary>
public sealed record ResourceRequestParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Resource URI being requested. Typically a `file:` URI on the receiver's
    /// filesystem, but any URI scheme that the receiver mediates access to is
    /// allowed.</summary>
    public required string Uri { get; init; }

    /// <summary>Whether the caller needs read access to the resource.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public bool? Read { get; init; }

    /// <summary>Whether the caller needs write access to the resource.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public bool? Write { get; init; }
}

/// <summary>Result of the `resourceRequest` command.
///
/// An empty object on success.</summary>
public sealed record ResourceRequestResult
{
}

/// <summary>Creates a resource watcher on the receiver's filesystem.
///
/// The receiver allocates an `ahp-resource-watch:/&lt;id&gt;` channel URI and
/// returns it on {@link CreateResourceWatchResult.channel}. The caller then
/// [`subscribe`](/specification/subscriptions#subscribe-request)s to that channel to receive
/// `resourceWatch/changed` actions over the standard action envelope.
///
/// The watch lifecycle is tied to subscription: when every subscriber has
/// unsubscribed (or the underlying connection drops), the receiver MUST
/// release the watcher. There is no explicit dispose command — `unsubscribe`
/// is the only handle the caller needs.
///
/// Like the rest of the `resource*` family, `createResourceWatch` is
/// symmetrical and MAY be sent in either direction. Access is gated through
/// the same permission flow as `resourceRead`/`resourceWrite`.</summary>
public sealed record CreateResourceWatchParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>URI to watch.</summary>
    public required string Uri { get; init; }

    /// <summary>If `true`, the receiver MUST report changes for descendants of `uri`.
    /// If `false` (default), only changes to `uri` itself — and, when `uri`
    /// is a directory, its direct children — are reported.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public bool? Recursive { get; init; }

    /// <summary>Glob patterns or paths relative to `uri` to exclude from reporting.
    /// Wrapped in `{ items }` for forward compatibility.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public JsonElement? Excludes { get; init; }

    /// <summary>Glob patterns or paths relative to `uri` to restrict reporting to.
    /// Omit to report every change under `uri` subject to `excludes`.
    /// Wrapped in `{ items }` for forward compatibility.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public JsonElement? Includes { get; init; }
}

/// <summary>Result of the `createResourceWatch` command.</summary>
public sealed record CreateResourceWatchResult
{
    /// <summary>Receiver-assigned watch channel URI (`ahp-resource-watch:/&lt;id&gt;`). The
    /// caller subscribes to this URI to start receiving change events and
    /// unsubscribes to release the watcher.</summary>
    public required string Channel { get; init; }
}

/// <summary>Requests that the host load older historical turns into a chat state.
///
/// The command result does not carry turns. Instead, before responding, the host
/// MUST dispatch `chat/turnsLoaded` to insert any loaded turns into the chat
/// channel's `turns` state, ahead of the already-loaded window, and update or
/// clear `turnsNextCursor`.
///
/// Before applying any operation that references a turn outside the currently
/// loaded window, the host MUST eagerly load enough older turns into state for
/// that operation to reduce against valid state.</summary>
public sealed record FetchTurnsParams
{
    /// <summary>Chat URI</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Opaque cursor from `ChatState.turnsNextCursor`.
    ///
    /// The host MUST reject unrecognised cursors with `InvalidParams`. Omit only
    /// when asking the host to opportunistically load its next older page for the
    /// chat, if any.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Cursor { get; init; }
}

/// <summary>Result of the `fetchTurns` command.</summary>
public sealed record FetchTurnsResult
{
}

/// <summary>Stop receiving updates for a channel.</summary>
public sealed record UnsubscribeParams
{
    /// <summary>Channel URI to unsubscribe from</summary>
    public required string Channel { get; init; }
}

/// <summary>Fire-and-forget action dispatch (write-ahead). The client applies actions
/// optimistically to local state and the server echoes them back as an
/// {@link ActionEnvelope} once accepted.
///
/// The client → server method is named `dispatchAction`; the server's reply
/// arrives on the server → client `action` notification (params:
/// {@link ActionEnvelope}).</summary>
public sealed record DispatchActionParams
{
    /// <summary>Channel URI this action targets</summary>
    public required string Channel { get; init; }

    /// <summary>Client sequence number</summary>
    public long ClientSeq { get; init; }

    /// <summary>The action to dispatch</summary>
    public required StateAction Action { get; init; }
}

/// <summary>Pushes a Bearer token for a protected resource. The `resource` field MUST
/// match a protected-resource identifier the client has discovered from the
/// server — whether declared statically in `AgentInfo.protectedResources`,
/// or discovered dynamically from a live `McpServerAuthRequiredState.resource`
/// or `ToolCallAuthRequiredState.auth.resource` (both surfaced only once the
/// corresponding MCP server or tool call actually challenges for auth).
/// Servers MUST accept any `resource` value they have themselves advertised
/// through one of these three mechanisms.
///
/// Tokens are delivered using [RFC 6750](https://datatracker.ietf.org/doc/html/rfc6750)
/// (Bearer Token Usage) semantics. The client obtains the token from the
/// authorization server(s) listed in the resource's metadata and pushes it
/// to the server via this command.</summary>
public sealed record AuthenticateParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>The protected resource identifier. MUST match a `resource` value the
    /// server has advertised — via `ProtectedResourceMetadata` in
    /// `AgentInfo.protectedResources`, or via a live
    /// `McpServerAuthRequiredState.resource` / `ToolCallAuthRequiredState.auth.resource`.</summary>
    public required string Resource { get; init; }

    /// <summary>Bearer token obtained from the resource's authorization server</summary>
    public required string Token { get; init; }

    /// <summary>The access token's remaining lifetime, in seconds, when this
    /// `authenticate` request is sent. This corresponds to `expires_in` in an
    /// OAuth 2.0 token response (RFC 6749 section 5.1).
    ///
    /// If the client retained the original token response, it MUST subtract the
    /// elapsed time before forwarding this value. Omit this field when the
    /// authorization server did not supply an expiry or the expiry is otherwise
    /// unknown. When supplied, the value MUST be a positive integer.
    ///
    /// This field is irrelevant when `token` is empty to revoke authentication
    /// and SHOULD be omitted in that case.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? ExpiresIn { get; init; }

    /// <summary>OAuth scopes the token grants, when known. Lets the server determine
    /// whether a specific challenge — e.g. the `requiredScopes` on a live
    /// `McpServerAuthRequiredState` or `ToolCallAuthRequiredState.auth` — is
    /// satisfied without decoding the (opaque, server-specific) token itself.
    /// Omit when the client doesn't track granted scopes separately from the
    /// token.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public List<string>? Scopes { get; init; }
}

/// <summary>Result of the `authenticate` command.
///
/// An empty object on success. If the token is invalid or the resource is
/// unrecognized, the server MUST return a JSON-RPC error (e.g. `AuthRequired`
/// `-32007` or `InvalidParams` `-32602`).</summary>
public sealed record AuthenticateResult
{
}

/// <summary>Creates a new terminal on the server.
///
/// After creation, the client should subscribe to the terminal URI to receive
/// state updates. The server dispatches `root/terminalsChanged` to update the
/// root terminal list.</summary>
public sealed record CreateTerminalParams
{
    /// <summary>Terminal URI (client-chosen).</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Initial owner of the terminal</summary>
    public required TerminalClaim Claim { get; init; }

    /// <summary>Human-readable terminal name</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Name { get; init; }

    /// <summary>Initial working directory URI</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Cwd { get; init; }

    /// <summary>Initial terminal width in columns</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? Cols { get; init; }

    /// <summary>Initial terminal height in rows</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? Rows { get; init; }
}

/// <summary>Disposes a terminal and kills its process if still running.
///
/// The server dispatches `root/terminalsChanged` to remove the terminal from
/// the root terminal list.</summary>
public sealed record DisposeTerminalParams
{
    /// <summary>Channel URI this command targets.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }
}

/// <summary>Iteratively resolves the session configuration schema. The client sends the
/// current partial session config and any user-filled metadata values. The server
/// returns a property schema describing what additional metadata is needed,
/// contextual to the current selections.
///
/// The client calls this command whenever the user changes a significant input
/// (e.g. picks a working directory, toggles a property). Each response returns
/// the full current property set (not a delta). The returned `values` contain
/// server-resolved defaults to pass to `createSession`.</summary>
public sealed record ResolveSessionConfigParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Agent provider ID</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Provider { get; init; }

    /// <summary>Working directory for the session</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? WorkingDirectory { get; init; }

    /// <summary>Current user-filled configuration values</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Config { get; init; }
}

/// <summary>Result of the `resolveSessionConfig` command.</summary>
public sealed record ResolveSessionConfigResult
{
    /// <summary>JSON Schema describing available configuration properties given the current context</summary>
    public required SessionConfigSchema Schema { get; init; }

    /// <summary>Current configuration values (echoed back with server-resolved defaults applied)</summary>
    public required Dictionary<string, JsonElement> Values { get; init; }
}

/// <summary>Queries the server for allowed values of a dynamic session config property.
///
/// Used when a property in the schema returned by `resolveSessionConfig` has
/// `enumDynamic: true`. The client sends a search query and receives matching
/// values with display metadata.</summary>
public sealed record SessionConfigCompletionsParams
{
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Agent provider ID</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Provider { get; init; }

    /// <summary>Working directory for the session</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? WorkingDirectory { get; init; }

    /// <summary>Current user-filled configuration values (provides context for the query)</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Config { get; init; }

    /// <summary>Property id from the schema to query values for</summary>
    public required string Property { get; init; }

    /// <summary>Search filter text (empty or omitted returns default/recent values)</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Query { get; init; }
}

/// <summary>Result of the `sessionConfigCompletions` command.</summary>
public sealed record SessionConfigCompletionsResult
{
    /// <summary>Matching value items</summary>
    public required List<SessionConfigValueItem> Items { get; init; }
}

/// <summary>A single value item returned by `sessionConfigCompletions`.</summary>
public sealed record SessionConfigValueItem
{
    /// <summary>The value to store in config</summary>
    public required string Value { get; init; }

    /// <summary>Human-readable display label</summary>
    public required string Label { get; init; }

    /// <summary>Optional secondary description</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Description { get; init; }
}

/// <summary>Requests completion items for a partially-typed input (e.g. a user message
/// the user is currently composing). Used to power `@`-mention pickers,
/// file/symbol references, and similar inline-completion experiences.
///
/// Servers SHOULD treat this command as best-effort and return promptly. The
/// client SHOULD debounce calls to avoid flooding the server with requests on
/// every keystroke.</summary>
public sealed record CompletionsParams
{
    /// <summary>The chat URI the completion is being requested for.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>What kind of completion is being requested.</summary>
    public CompletionItemKind Kind { get; init; }

    /// <summary>The complete text of the input being completed (e.g. the full user
    /// message text typed so far).</summary>
    public required string Text { get; init; }

    /// <summary>The character offset within `text` at which the completion is requested,
    /// measured in UTF-16 code units. MUST satisfy `0 &lt;= offset &lt;= text.length`.</summary>
    public long Offset { get; init; }
}

/// <summary>A single completion item returned by the `completions` command.
///
/// When the user accepts an item, the client SHOULD:
/// 1. Replace the range `[rangeStart, rangeEnd)` in the input with `insertText`
///    (or insert `insertText` at the cursor when the range is omitted).
/// 2. Associate the item's `attachment` with the resulting {@link Message}.</summary>
public sealed record CompletionItem
{
    /// <summary>The text inserted into the input when this item is accepted.</summary>
    public required string InsertText { get; init; }

    /// <summary>If defined, the start of the range in the input's `text` that is replaced
    /// by `insertText`. The range is the half-open interval
    /// `[rangeStart, rangeEnd)` of character offsets, measured in UTF-16 code
    /// units.
    ///
    /// When omitted, the client SHOULD insert `insertText` at the cursor.
    ///
    /// Note: this range refers to positions in the *current* input. The
    /// attachment's own `rangeStart`/`rangeEnd` (when present) refer to
    /// positions in the final {@link Message.text} after the item is
    /// accepted.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? RangeStart { get; init; }

    /// <summary>The end of the range in the input's `text` that is replaced by
    /// `insertText`. See {@link rangeStart}.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? RangeEnd { get; init; }

    /// <summary>The attachment associated with this completion item.</summary>
    public required MessageAttachment Attachment { get; init; }
}

/// <summary>Result of the `completions` command.</summary>
public sealed record CompletionsResult
{
    /// <summary>The completion items, in the order the server suggests displaying them.</summary>
    public required List<CompletionItem> Items { get; init; }
}

/// <summary>Invokes a server-defined {@link ChangesetOperation} against a changeset,
/// a single file, or a line range.
///
/// The server validates that `operationId` exists in the changeset's
/// current `operations` list and that the requested `target.kind` is
/// contained in the operation's `scopes`. Invalid combinations result in a
/// JSON-RPC error.
///
/// State changes resulting from invocation flow back through the normal
/// `changeset/*` action stream on the relevant changeset URIs. Clients
/// SHOULD NOT synthesise local optimistic changes for invocations unless
/// the server explicitly opts in via a future capability.</summary>
public sealed record InvokeChangesetOperationParams
{
    /// <summary>The expanded changeset URI.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Matches {@link ChangesetOperation.id} from the changeset's `operations` list.</summary>
    public required string OperationId { get; init; }

    /// <summary>Target of the operation. Required iff the chosen scope is
    /// `'resource'` or `'range'`. Omit for changeset-scoped operations.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public ChangesetOperationTarget? Target { get; init; }
}

/// <summary>Result of the {@link InvokeChangesetOperationParams | `invokeChangesetOperation`}
/// command.
///
/// Success is implicit: the server returns this result when it accepted
/// the operation. Failure is signalled by rejecting the JSON-RPC request
/// with an appropriate error code, not by any field on this result. The
/// operation MAY still produce subsequent failure feedback through the
/// {@link ChangesetStatusChangedAction | `changeset/statusChanged`} stream.</summary>
public sealed record InvokeChangesetOperationResult
{
    /// <summary>Optional human-readable message describing the result.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public StringOrMarkdown? Message { get; init; }

    /// <summary>Optional follow-up: a URI to open (e.g. a PR), a content ref, etc.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public ChangesetOperationFollowUp? FollowUp { get; init; }
}

/// <summary>Optional follow-up surfaced by the server after an operation completes —
/// a {@link ContentRef} the client can fetch and display.
///
/// Set `external` to `true` to open the content in the user's preferred
/// external handler (e.g. browser); otherwise the client is expected to
/// surface it inline.</summary>
public sealed record ChangesetOperationFollowUp
{
    public required ContentRef Content { get; init; }

    /// <summary>When `true`, open in an external handler rather than inline.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public bool? External { get; init; }
}

/// <summary>Discover event-trigger types available for a prospective session template.
///
/// Hosts may vary definitions by provider, workspace, and session
/// configuration. Schedule triggers are protocol-defined and therefore do not
/// appear in this result. The result describes current authoring and validation
/// choices. Saved {@link AutomationEventTrigger} values retain their selected
/// event descriptors for display but do not establish current availability.</summary>
public sealed record ListAutomationTriggerDefinitionsParams
{
    /// <summary>Trigger definitions are discovered from the root channel.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Prospective provider id matching {@link AgentInfo.provider}, or omitted for the host default.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Provider { get; init; }

    /// <summary>Prospective {@link AutomationSessionTemplate.workingDirectories}.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public List<string>? WorkingDirectories { get; init; }

    /// <summary>Prospective resolved {@link AutomationSessionTemplate.config}.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? SessionConfig { get; init; }
}

/// <summary>Host-defined event trigger types available for the supplied context.</summary>
public sealed record ListAutomationTriggerDefinitionsResult
{
    /// <summary>Available event trigger definitions.</summary>
    public required List<AutomationTriggerDefinition> Items { get; init; }
}

/// <summary>Start a manual run of an automation.
///
/// Manual execution is independent of {@link AutomationDefinition.enabled}.
/// The host persists the run before beginning session side effects.</summary>
public sealed record RunAutomationParams
{
    /// <summary>Manual runs are scoped to the catalogue channel.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Target {@link AutomationEntry.resource}.</summary>
    public required string Automation { get; init; }

    /// <summary>Durable client-generated idempotency key. Retrying with the same key and
    /// automation MUST return the original run URI rather than create another
    /// run.</summary>
    public required string RequestId { get; init; }
}

/// <summary>Result identifying the existing or newly created run.</summary>
public sealed record RunAutomationResult
{
    /// <summary>Subscribable `ahp-automation-run:` URI matching {@link AutomationRunState.resource}.</summary>
    public required string Resource { get; init; }
}

/// <summary>Load one older page into a catalogued automation's run-history state.
///
/// The response only acknowledges the request. The updated full state arrives
/// through {@link AutomationSetAction | `automation/set`} on the
/// `ahp-automations://` channel, keeping all catalogue subscribers synchronized
/// through the normal action stream.</summary>
public sealed record FetchAutomationRunsParams
{
    /// <summary>Run-history loading is scoped to the catalogue channel.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Target {@link AutomationEntry.resource}.</summary>
    public required string Automation { get; init; }

    /// <summary>Cursor previously received as {@link AutomationEntry.runsNextCursor}.
    /// Omit to request the first page not already included by the snapshot.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Cursor { get; init; }
}

/// <summary>Empty acknowledgement; the updated automation state is delivered by action.</summary>
public sealed record FetchAutomationRunsResult
{
}

/// <summary>Discovers canvas TYPES currently available to open for one exact backing
/// chat.
///
/// This is a **pure read/browse** operation: it MUST NOT open, materialize,
/// or otherwise admit any canvas — see `openCanvas` for that. It is
/// unrelated to {@link SessionState.canvases}, which reflects durable
/// membership of already-opened canvas INSTANCES, not the set of canvas
/// TYPES a host/extension could open; do not confuse the two.</summary>
public sealed record ListCanvasTypesParams
{
    /// <summary>The exact backing chat to discover available canvas types for.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Maximum number of entries to return in this page. The server SHOULD respect
    /// this bound but MAY return fewer entries and MAY impose its own upper cap.
    /// Omit to let the server choose the page size.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public long? Limit { get; init; }

    /// <summary>Opaque pagination cursor from a previous {@link PaginatedResult.nextCursor}.
    /// Omit to fetch the first page. Cursors are server-defined and MUST be treated
    /// as opaque — do not parse, modify, or persist them across connections. An
    /// unrecognised cursor SHOULD be rejected with an `InvalidParams` error.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Cursor { get; init; }
}

/// <summary>Available canvas types for the requested chat.</summary>
public sealed record ListCanvasTypesResult
{
    /// <summary>Opaque cursor for the next page. Present when more entries exist beyond the
    /// returned page; absent signals the end of the collection. Pass it back as
    /// {@link PaginatedParams.cursor} to fetch the following page.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? NextCursor { get; init; }

    /// <summary>Discovered canvas type declarations.</summary>
    public required List<CanvasTypeDeclaration> Types { get; init; }
}

/// <summary>Explicitly opens (admits) a canvas, associating it with the owning chat
/// given by `identity.chat` at the moment of the call — never with whichever
/// chat later happens to have focus.
///
/// This is a read-write admission, not a resolve: unlike `subscribe` (which
/// only reads current state), `openCanvas` is the operation that creates
/// durable membership. There is no implicit open — a client MUST call this
/// before a canvas appears in {@link SessionState.canvases}. Once admitted,
/// clients read and follow live state by `subscribe`-ing to the returned
/// `canvas.resource`, and resolve the current live endpoint via
/// `resolveCanvasSource`; neither read itself opens, resumes, or restarts
/// anything.
///
/// **Logical identity is always singular.** The same {@link CanvasIdentityKey}
/// (`chat`, `source`, `canvasType`, `instanceId`) always resolves to the same
/// `canvas` resource URI and the same {@link SessionState.canvases} catalog
/// entry, no matter how many times `openCanvas` is called for it — the server
/// MUST return that existing entry's `resource` rather than mint a second
/// one. A client-supplied `canvas` URI is honored only on the call that first
/// establishes the identity; on a later call for an already-recorded
/// identity the server MUST ignore the supplied `canvas` value and return the
/// existing resource instead.
///
/// **Idempotency is scoped to `requestId`, not identity.** Retrying with the
/// exact same `requestId` and byte-for-byte identical params from the same
/// authenticated connection MUST return the original result without
/// repeating any side effect, within a bounded live window (the server is
/// not required to remember it forever). Reusing the same `requestId` with
/// any different parameter value MUST be rejected with `Conflict`
/// (`-32011`) — mint a new `requestId` for a new logical call. A genuinely
/// NEW `requestId` for an already-open identity MAY be effectful (e.g.
/// updating `title`/`icon`, or causing the provider to re-run its own
/// open-time initialization with new `input`) — this mirrors the pinned
/// SDK's own repeated-open behavior and does not create a second logical
/// identity. There is no exactly-once-across-crash guarantee: a lost reply
/// is indeterminate, and clients MUST NOT automatically replay `openCanvas`
/// — reconnect and read `SessionState.canvases` / `resolveCanvasSource`
/// instead to determine the actual outcome.</summary>
public sealed record OpenCanvasParams
{
    /// <summary>Session URI that will list the opened canvas in `SessionState.canvases`.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Canvas URI (client-chosen, e.g. `ahp-canvas:/&lt;uuid&gt;`); honored only when this call first establishes `identity` — see above.</summary>
    public required string Canvas { get; init; }

    /// <summary>Logical identity to open or re-admit.</summary>
    public required CanvasIdentityKey Identity { get; init; }

    /// <summary>Initial (or updated, on a later effectful call) display title.</summary>
    public required string Title { get; init; }

    /// <summary>Initial (or updated) display icon.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Icon? Icon { get; init; }

    /// <summary>Bounded JSON input for this open call (e.g. seed parameters the
    /// provider uses to initialize the canvas), opaque to the protocol. See
    /// {@link CanvasTypeDeclaration.openInputSchema} /
    /// `openInputSchemaRef` for the expected shape. The JSON-serialized value
    /// MUST NOT exceed `CANVAS_INPUT_MAX_LENGTH`.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public JsonElement? Input { get; init; }

    /// <summary>Durable client-generated idempotency key bounding retry deduplication
    /// for this call within a live window; see the idempotency rules above.
    /// MUST NOT exceed `CANVAS_REQUEST_ID_MAX_LENGTH`.</summary>
    public required string RequestId { get; init; }
}

/// <summary>Result identifying the existing or newly opened canvas.</summary>
public sealed record OpenCanvasResult
{
    /// <summary>The catalog entry for the opened (or already-open) canvas.</summary>
    public required CanvasEntry Canvas { get; init; }
}

/// <summary>Pure, read-only read of a canvas's current live-resolution state and,
/// when currently live, a transient endpoint presentation.
///
/// This MUST NOT create, resume, reopen, or restart a provider. If the
/// canvas does not currently have a live endpoint, `source` is absent and
/// `availability` reflects why (e.g. `notLoaded`, `loading`, `failed`) —
/// call `restartCanvasProvider` (an explicitly effectful operation) to
/// attempt recovery instead. A client-local page reload (re-navigating the
/// client's own rendering surface to the same still-live `source.url`)
/// needs no dedicated command at all; calling `resolveCanvasSource` again is
/// also how a client retries resolving a currently-unavailable source
/// without restarting anything.</summary>
public sealed record ResolveCanvasSourceParams
{
    /// <summary>The canvas URI (an already-opened canvas's `resource`).</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }
}

/// <summary>The canvas's current live-resolution state as of this read.</summary>
public sealed record ResolveCanvasSourceResult
{
    /// <summary>Current {@link CanvasEntry.availability}.</summary>
    public CanvasAvailabilityStatus Availability { get; init; }

    /// <summary>Current {@link CanvasIdentity.incarnation}.</summary>
    public required string Incarnation { get; init; }

    /// <summary>Current {@link CanvasEntry.revision}.</summary>
    public long Revision { get; init; }

    /// <summary>Present only when a live endpoint currently exists (`availability` is `empty` or `ready`); absent otherwise. Transient — see {@link CanvasSourcePresentation}.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public CanvasSourcePresentation? Source { get; init; }
}

/// <summary>Invokes one of a canvas's currently declared actions exactly once.
///
/// The server MUST reject with `PermissionDenied` (`-32009`) if the canvas's
/// current trust is not `trusted`, and with `NotFound` (`-32008`) if
/// `actionId` does not match a currently declared action. `incarnation` is
/// REQUIRED — omitting stale-generation protection on an effectful call is
/// not allowed. If it does not match the canvas's current
/// {@link CanvasIdentity.incarnation}, the server MUST reject with `Conflict`
/// (`-32011`) rather than route the call to a superseded endpoint.
///
/// The result is the provider's raw reply and is never persisted into
/// `CanvasState` — large or provider-specific payloads stay off the durable
/// state tree; a reply that would exceed `CANVAS_RESULT_MAX_LENGTH` MUST be
/// represented out of band instead of being returned inline. Any resulting
/// state changes (e.g. a subsequent availability transition) flow back
/// separately through the normal `canvas/*` action stream on the canvas's
/// own channel.
///
/// A lost reply (e.g. a dropped connection after the provider already ran
/// the handler) is **indeterminate**: clients MUST NOT automatically replay
/// `invokeCanvasAction` on reconnect. Instead, reconnect and read the
/// canvas's current state (e.g. via `subscribe` / `resolveCanvasSource`) and
/// decide from observed `revision`/`incarnation` and any provider-visible
/// side effect whether to surface the ambiguity to the user, rather than
/// assuming success or failure.</summary>
public sealed record InvokeCanvasActionParams
{
    /// <summary>The canvas URI.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Matches a {@link CanvasActionDeclaration.id} from the canvas's current declared actions.</summary>
    public required string ActionId { get; init; }

    /// <summary>Input conforming to the declared action's `inputSchema`/`inputSchemaRef`,
    /// if any. The JSON-serialized value MUST NOT exceed
    /// `CANVAS_INPUT_MAX_LENGTH`.</summary>
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public JsonElement? Input { get; init; }

    /// <summary>Expected {@link CanvasIdentity.incarnation}. Required — see above. The
    /// server MUST reject the call with `Conflict` if the canvas's live
    /// endpoint has since been superseded, rather than deliver the call to it.</summary>
    public required string Incarnation { get; init; }

    /// <summary>Durable client-generated idempotency key bounding retry
    /// deduplication for this invocation within a live window. The server is
    /// not required to guarantee exactly-once execution across a crash. MUST
    /// NOT exceed `CANVAS_REQUEST_ID_MAX_LENGTH`.</summary>
    public required string RequestId { get; init; }
}

/// <summary>Result of invoking a declared canvas action.</summary>
public sealed record InvokeCanvasActionResult
{
    /// <summary>The provider's raw reply, opaque to the protocol. MUST NOT exceed `CANVAS_RESULT_MAX_LENGTH` once JSON-serialized.</summary>
    public JsonElement Result { get; init; }
}

/// <summary>Explicitly restarts the provider/chat-scoped runtime backing this canvas:
/// retires the current live endpoint and establishes a fresh one for the
/// same logical instance.
///
/// This is the **only** operation that intentionally causes an
/// {@link CanvasIncarnationChangedAction | incarnation bump}; `resolveCanvasSource`
/// (read-only source resolution / client-local page reload) MUST NEVER
/// trigger it. The host dispatches {@link CanvasAvailabilityChangedAction}
/// (transitioning through `notLoaded`/`loading`) and then
/// {@link CanvasIncarnationChangedAction} to reflect the outcome. Restart
/// never replays a prior `invokeCanvasAction`, and MUST NOT steal focus or
/// restore any prior in-flight effect.
///
/// `incarnation` is REQUIRED: the server MUST reject with `Conflict`
/// (`-32011`) if it does not match the canvas's current
/// {@link CanvasIdentity.incarnation}, so a caller cannot restart a
/// generation it never observed (e.g. after racing a concurrent restart). A
/// lost reply is indeterminate; clients MUST NOT automatically replay this
/// command — reconnect and compare the canvas's current `incarnation`
/// instead.</summary>
public sealed record RestartCanvasProviderParams
{
    /// <summary>The canvas URI.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Durable client-generated idempotency key, following the same
    /// requestId-scoped idempotency rules as `openCanvas`. MUST NOT exceed
    /// `CANVAS_REQUEST_ID_MAX_LENGTH`.</summary>
    public required string RequestId { get; init; }

    /// <summary>Expected current {@link CanvasIdentity.incarnation}; required — see above.</summary>
    public required string Incarnation { get; init; }
}

/// <summary>Logically closes a canvas: removes its durable membership from
/// `SessionState.canvases` and disposes matching views.
///
/// This is distinct from a client merely hiding a local tab or view, which is
/// presentation-only and MUST NOT dispatch this command. There is no
/// advertised model tool for this operation — it is invoked only by
/// UI/RPC callers.
///
/// `revision` is REQUIRED: the server MUST reject with `Conflict`
/// (`-32011`) if it does not match the canvas's current
/// {@link CanvasEntry.revision}, so a caller cannot close membership state it
/// never actually observed. If no matching entry exists (e.g. already
/// closed), the server MUST treat this as a successful no-op rather than an
/// error — the `revision` precondition only applies when an entry still
/// exists. A lost reply is indeterminate; clients MUST NOT automatically
/// replay this command — reconnect and check `SessionState.canvases`
/// instead.</summary>
public sealed record CloseCanvasParams
{
    /// <summary>The canvas URI.</summary>
    public required string Channel { get; init; }

    /// <summary>Optional JSON-serializable metadata associated with this request.
    /// Receivers MUST ignore keys they do not understand.</summary>
    [JsonPropertyName("_meta")]
    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public Dictionary<string, JsonElement>? Meta { get; init; }

    /// <summary>Durable client-generated idempotency key, following the same
    /// requestId-scoped idempotency rules as `openCanvas`. MUST NOT exceed
    /// `CANVAS_REQUEST_ID_MAX_LENGTH`.</summary>
    public required string RequestId { get; init; }

    /// <summary>Expected current {@link CanvasEntry.revision}; required when an entry still exists — see above.</summary>
    public long Revision { get; init; }
}

// ─── ReconnectResult Union ────────────────────────────────────────────

/// <summary>ReconnectResult is the result of the `reconnect` command.</summary>
[JsonConverter(typeof(ReconnectResultConverter))]
public sealed class ReconnectResult : AhpUnion
{
    /// <summary>Creates an empty ReconnectResult (no active variant).</summary>
    public ReconnectResult() { }

    /// <summary>Creates a ReconnectResult wrapping the given variant value.</summary>
    public ReconnectResult(object? value) : base(value) { }
}

/// <summary>System.Text.Json converter for the ReconnectResult discriminated union.</summary>
internal sealed class ReconnectResultConverter : UnionConverter<ReconnectResult>
{
    public ReconnectResultConverter()
        : base(
            discriminator: "type",
            variants: new Dictionary<string, Type>
            {
        ["replay"] = typeof(ReconnectReplayResult),
        ["snapshot"] = typeof(ReconnectSnapshotResult),
            },
            allowUnknown: false)
    {
    }
}
/// <summary>ChatSource describes how a new chat uses its source chat and turn.</summary>
[JsonConverter(typeof(ChatSourceConverter))]
public sealed class ChatSource : AhpUnion
{
    /// <summary>Creates an empty ChatSource (no active variant).</summary>
    public ChatSource() { }

    /// <summary>Creates a ChatSource wrapping the given variant value.</summary>
    public ChatSource(object? value) : base(value) { }
}

/// <summary>System.Text.Json converter for the ChatSource discriminated union.</summary>
internal sealed class ChatSourceConverter : UnionConverter<ChatSource>
{
    public ChatSourceConverter()
        : base(
            discriminator: "kind",
            variants: new Dictionary<string, Type>
            {
        ["fork"] = typeof(ForkChatSource),
        ["sideChat"] = typeof(SideChatSource),
            },
            allowUnknown: false)
    {
    }
}

// ─── Changeset Operation Unions ───────────────────────────────────────

/// <summary>
/// ChangesetOperationTarget identifies the file or range a
/// ChangesetOperation should act on.
/// </summary>
[JsonConverter(typeof(ChangesetOperationTargetConverter))]
public sealed class ChangesetOperationTarget : AhpUnion
{
    public ChangesetOperationTarget() { }
    public ChangesetOperationTarget(object? value) : base(value) { }
}

/// <summary>Targets an entire resource.</summary>
public sealed record ChangesetOperationResourceTarget
{
    public string Kind { get; init; } = "resource";

    public required string Resource { get; init; }

    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Side { get; init; }
}

/// <summary>Targets a range within a resource.</summary>
public sealed record ChangesetOperationRangeTarget
{
    public string Kind { get; init; } = "range";

    public required string Resource { get; init; }

    [JsonIgnore(Condition = JsonIgnoreCondition.WhenWritingNull)]
    public string? Side { get; init; }

    public required TextRange Range { get; init; }
}

/// <summary>System.Text.Json converter for the ChangesetOperationTarget union.</summary>
internal sealed class ChangesetOperationTargetConverter : UnionConverter<ChangesetOperationTarget>
{
    public ChangesetOperationTargetConverter()
        : base(
            discriminator: "kind",
            variants: new Dictionary<string, Type>
            {
                ["resource"] = typeof(ChangesetOperationResourceTarget),
                ["range"] = typeof(ChangesetOperationRangeTarget),
            },
            allowUnknown: false)
    {
    }
}
